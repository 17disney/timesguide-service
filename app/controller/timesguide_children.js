const Controller = require('egg').Controller

class TimesguideChildrenController extends Controller {
  async list() {
    const { ctx } = this.ctx
    // const { status } = ctx.query
  }
  async id() {}
}

module.exports = TimesguideChildrenController
